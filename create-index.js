const { Pinecone } = require('@pinecone-database/pinecone');
async function createPineconeIndex() { try { const pinecone = new Pinecone({ apiKey: 'pcsk_7334cs_HVm39trGw84ZhKCPjdEond8FeaaEwirBDeiTxkacMLLMZaFbyEFuFccvjUEyXus' }); console.log('Creating index: chat-brilliant-v4...'); await pinecone.createIndex({ name: 'chat-brilliant-v4', dimension: 1536, metric: 'cosine', spec: { serverless: { cloud: 'aws', region: 'us-east-1' } } }); console.log('Index created successfully!'); console.log('Waiting for 60 seconds for the index to initialize...'); await new Promise(resolve => setTimeout(resolve, 60000)); console.log('Listing all indexes...'); const indexes = await pinecone.listIndexes(); console.log('Available indexes:', indexes); } catch (error) { console.error('Error creating Pinecone index:', error); } } createPineconeIndex();
