const { Pinecone } = require('@pinecone-database/pinecone'); async function testPinecone() { try { console.log('Testing Pinecone connectivity...'); const pinecone = new Pinecone({ apiKey: process.env.PINECONE_API_KEY }); const indexes = await pinecone.listIndexes(); console.log('Available indexes:', indexes); const index = pinecone.Index('chat-brilliant-v4'); const stats = await index.describeIndexStats(); console.log('Index stats:', stats); console.log('Pinecone connectivity test successful!'); } catch (error) { console.error('Pinecone connectivity error:', error); } } testPinecone();
